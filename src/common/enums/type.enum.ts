export enum AuthTypes {
    LOGIN = "login",
    REGISTER = "register"
}
export enum AUTH_RESULTS_ENUM {
    LOGIN = "login",
    REGISTER = "register",
    FORGOT_PASS = "forgot_pass"
}

export enum TOKEN_TYPE {
    FORGOTPASS = "forgot_pass",
    VERIFY = "verify",
    CHANGE_EMAIL = "change_email"
}